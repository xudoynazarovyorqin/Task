export const getters = {
    list: state => state.list,
    comingProducts: state => state.comingProducts,
    columns: state => state.columns,
    filter: state => state.filter,
    pagination: state => state.pagination,
    sort: state => state.sort
};